[defaults]
inventory       = inventory.ini
remote_user     = aya
host_key_checking = False
timeout         = 30

[privilege_escalation]
become          = True
become_method   = sudo
become_user     = root
