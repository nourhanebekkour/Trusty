# Environnement de Test
NODE_ENV=test
PORT=3001

# Base de données de test (DOIT ÊTRE DIFFÉRENTE DE DEV)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://trusty_user:trusty_password@localhost:5432/trusty_test?schema=public"

# Authentification
JWT_SECRET="test_secret_key_123"
JWT_REFRESH_SECRET="test_refresh_secret_key_456"
JWT_EXPIRES_IN="1h"

# Services
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test

# Frontend
FRONTEND_URL=http://localhost:5173

# Minio (Test)
MINIO_ENDPOINT=localhost
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_PUBLIC_BUCKET=test-public
MINIO_PRIVATE_BUCKET=test-private
