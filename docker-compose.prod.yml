#──────────────────────────────── OVERRIDE PROD ────────────────────────────────

services:

  frontend:
    build:
      target: production
    ports:
      - "8080:80"
      # meme port que staging, ils seront dans des differents VMs
    environment:
      - NODE_ENV=production
      - VITE_API_URL=http://${VM2_IP}:3000
    restart: always

  backend:
    build:
      target: production
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:5432/${POSTGRES_DB}
      - JWT_SECRET=${JWT_SECRET}
    restart: always

  database:
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    restart: always


# Commande : docker-compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env.prod up -d
